"""
Inventory purchases (restocks) are handled via POST /inventory/{id}/restock.
This router is a placeholder to satisfy the project structure spec.
"""
from fastapi import APIRouter

router = APIRouter()
